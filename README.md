# react_playground
## Build the React app:
cd client && npm run build

## Test the React app:
cd client && npm test

## Start the server:
cd ../server && node index.js

this should start the server on port 5001